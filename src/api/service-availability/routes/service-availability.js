'use strict';

/**
 * service-availability router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-availability.service-availability');
